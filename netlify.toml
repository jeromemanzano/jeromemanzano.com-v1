# example netlify.toml
[build]
  command = "vite build"
  functions = "server/functions"
  publish = "dist"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200

  from = "/*"
  to = "/index.html"
  status = 200
